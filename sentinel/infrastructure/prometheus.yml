global:
  scrape_interval: 15s
  evaluation_interval: 30s
  external_labels: { deployment: sentinel-demo }

scrape_configs:
  - job_name: sentinel-api
    metrics_path: /metrics
    static_configs:
      - targets: ['api:8000']
        labels: { service: api }
