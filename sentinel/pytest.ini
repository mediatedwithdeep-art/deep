[pytest]
testpaths = tests
pythonpath = shared ai backend video-ingestion
markers =
    integration: requires a live PostgreSQL
    slow: takes more than a second
filterwarnings = ignore::DeprecationWarning
